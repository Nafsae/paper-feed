# 精确关键词配置 - 仅匹配指定数据集
# 格式: keyword1 AND keyword2 NOT excluded1 NOT excluded2

# ===== 息肉分割 (Polyp) =====
polyp NOT semi-supervised NOT self-supervised NOT weakly-supervised NOT unsupervised
colonoscopy AND segmentation NOT semi-supervised NOT self-supervised NOT weakly-supervised NOT unsupervised
Kvasir-SEG NOT semi-supervised NOT self-supervised
CVC-ClinicDB NOT semi-supervised NOT self-supervised
CVC-ColonDB NOT semi-supervised NOT self-supervised
ETIS-LaribPolypDB NOT semi-supervised NOT self-supervised

# ===== 乳腺超声 (BUSI) =====
BUSI NOT semi-supervised NOT self-supervised NOT weakly-supervised NOT unsupervised
breast AND ultrasound AND segmentation NOT semi-supervised NOT self-supervised NOT weakly-supervised NOT unsupervised

# ===== 皮肤病变 (ISIC) =====
ISIC NOT semi-supervised NOT self-supervised NOT weakly-supervised NOT unsupervised
skin AND lesion AND segmentation NOT semi-supervised NOT self-supervised NOT weakly-supervised NOT unsupervised
dermoscopy AND segmentation NOT semi-supervised NOT self-supervised NOT weakly-supervised NOT unsupervised

# ===== 腺体分割 (GlaS) =====
GlaS NOT semi-supervised NOT self-supervised NOT weakly-supervised NOT unsupervised
gland AND segmentation NOT semi-supervised NOT self-supervised NOT weakly-supervised NOT unsupervised
